# 🧮 CRUD App (PHP + MySQL)

A simple CRUD (Create, Read, Update, Delete) web application built using **PHP** and **MySQL**.  
This project demonstrates the core functionality of database-driven web apps — adding, editing, deleting, and viewing records.

---

## 🚀 Features
- Add new records  
- Edit existing data  
- Delete entries from the database  
- View all items in a clean table layout  
- Responsive and simple user interface  

---

## 🛠️ Tech Stack
- **Frontend:** HTML, CSS  
- **Backend:** PHP  
- **Database:** MySQL  

---

## ⚙️ How It Works
1. Connect to the database using `conn.php`.  
2. Display data using `index.php`.  
3. Add new entries through `insert.php`.  
4. Update records via `update.php`.  
5. Delete items using `delete.php`.

---

## 📸 Screenshot
*(Add a screenshot of your app here if available)*  
Example:
```bash
![CRUD App Screenshot](screenshot.png)
